input {
  tcp {
    port => 5000
    codec => json
  }
  udp {
    port => 5000
    codec => json
  }
  beats {
    port => 5044
  }
}

filter {
  # Simple parser filters can go here for enrichment
  if [type] == "wazuh-alerts" {
    # Custom wazuh parsing
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "vigilance-alerts-%{+YYYY.MM.dd}"
  }
}
